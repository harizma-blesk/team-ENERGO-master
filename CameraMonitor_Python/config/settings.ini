# Конфигурационный файл для Camera Monitor
# Скопируйте settings.ini.example в settings.ini и отредактируйте

[Database]
# Путь к SQLite БД файлу
path=camera_monitor.db

[Camera1]
rtspUrl=rtsp://login:pass@192.168.1.10:554/stream
auditoryName=А-101
cameraName=Camera_A101

[Camera2]
rtspUrl=rtsp://login:pass@192.168.1.11:554/stream
auditoryName=Б-205
cameraName=Camera_B205

[UDP]
# IP сервера Python backend
IP_PythonServer=127.0.0.1
# Порт для слушания входящих сообщений
IP_Port_Listen=5000
# Порт для отправки сообщений
IP_Port_Send=5001
# Удалённый порт (резервное)
IP_Port_Remote=5002

[NEUROMODEL]
# Путь к YOLO модели (скачивается автоматически)
WeightsPath=models/yolov8n.onnx
# Порог уверенности для детекции
ConfThreshold=0.5
# Путь к файлу с названиями классов COCO
CocoNamesPath=data/coco.names

[TCP_Servers]
IP_Java = 127.0.0.1
PORT_Java = 0
IP_ESP = 127.0.0.1
PORT_ESP = 0

[Logging]
# Уровень логирования: DEBUG, INFO, WARNING, ERROR
level=INFO
# Формат логов
format=%(asctime)s - %(name)s - %(levelname)s - %(message)s
# Директория для логов
log_dir=logs

[GUI]
# Ширина главного окна
window_width=1200
# Высота главного окна
window_height=800
# Целевой FPS для обновления видео
fps_target=30

[Laravel]
# Включить отправку статуса занятости с вебки в php-server
enabled=true
# Laravel API base URL
baseUrl=http://127.0.0.1:3333/api
# Адрес камеры, при локальной вебке можно оставить webcam://0
cameraAddress=webcam://0
# Порт камеры, если для всех один порт - укажите его здесь
cameraPort=0
# Как часто слать обновление, если статус не меняется
syncIntervalSeconds=2
# HTTP таймаут
timeoutSeconds=3